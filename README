There isn't much to say here. Put these files in your 'www' directory and you should have an instance of Startup Map going.

Inside of the 'backend' directory you'll find a few python scripts that automate the fetching of the latest data from Crunchbase.
